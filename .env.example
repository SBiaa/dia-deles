DATABASE_URL=

# gerar com: npx auth secret (ou: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))")
AUTH_SECRET=

# token do Vercel Blob store deste projeto (Vercel dashboard > Storage > Blob)
BLOB_READ_WRITE_TOKEN=
